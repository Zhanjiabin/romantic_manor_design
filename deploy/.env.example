# 复制到仓库根目录，改名为 .env
#   copy deploy\.env.example .env

# 有域名（推荐，自动 HTTPS）：
# MANOR_SITE=https://desk.example.com
# 没有域名、先用 IP 访问（仅 HTTP，密码会明文走网，只适合临时试）：
MANOR_SITE=http://:80

MANOR_USER=manor
MANOR_PASSWORD=请改成很长的随机密码
